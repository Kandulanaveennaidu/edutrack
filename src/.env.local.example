# EduTrack Environment Variables Template
# Copy this file to .env.local and fill in the values

# Google Sheets Configuration
# Create a service account at https://console.cloud.google.com/
# Enable Google Sheets API and download the JSON key file
GOOGLE_SHEETS_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
GOOGLE_SHEETS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Google Spreadsheet ID
# Create a new Google Sheet and share it with the service account email
# The ID is in the URL: https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
GOOGLE_SHEETS_ID=your-spreadsheet-id-here

# NextAuth.js Configuration
# For local development, use http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Generate a secure random string for JWT signing
# Run: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-here

# ===========================================
# Google Sheets Setup Instructions:
# ===========================================
# 
# Create the following sheets in your Google Spreadsheet:
#
# 1. "Schools" sheet with columns:
#    id | name | address | phone | email | logo_url | created_at | updated_at
#
# 2. "Teachers" sheet with columns:
#    id | school_id | name | email | password | role | avatar_url | created_at | updated_at
#
# 3. "Students" sheet with columns:
#    id | school_id | roll_number | name | class | section | parent_name | parent_phone | email | avatar_url | status | created_at | updated_at
#
# 4. "Attendance" sheet with columns:
#    id | school_id | student_id | date | status | marked_by | remarks | created_at | updated_at
#
# 5. "LeaveRequests" sheet with columns:
#    id | school_id | student_id | start_date | end_date | reason | status | approved_by | created_at | updated_at
#
# 6. "Settings" sheet with columns:
#    id | school_id | working_days | class_timings | attendance_cutoff | notifications_enabled | created_at | updated_at
#
# Add header row to each sheet with the column names listed above.
# Share the spreadsheet with your service account email address.
